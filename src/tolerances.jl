# the tolerance to use when comparing float expressions, e.g.,
# x + y * z <= 0 turns into x + y * z < EXPRESSION_TOL
const EXPRESSION_TOL = 1e-7

# the tolerance to use when storing float values, e.g.,
# instead of setting a variable x = 1e-11, we set x = 0
const VARIABLE_TOL = 1e-10
